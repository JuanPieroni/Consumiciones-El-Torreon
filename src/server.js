import { log, info, success, warn, error } from "console-log-plus"
const a = "letra"
console.log(a)

log("normal log")
info("Información")
success("Éxito total!")
warn("Advertencia")
error("Error grave")















































